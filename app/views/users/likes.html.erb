<div class="row">
  <div class="col-xs-12">
    <div class="<%= 'active' if current_page?(user_path(@user)) %>"><%= link_to likes_user_path(@user.id) do %>Likes<% end %></div>
    <% @microposts.each do |micropost| %>
      <%= micropost.id %>
    <% end %>
  </div>
</div>